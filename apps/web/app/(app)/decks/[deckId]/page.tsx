/** Deck detail page placeholder — Phase 6. */
export default function DeckDetailPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-foreground">Deck</h1>
      <p className="mt-2 text-foreground-muted">
        Card grid for a single deck — implemented in Phase 6.
      </p>
    </div>
  );
}
