{
  "name": "@lumora/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Lumora Tauri v2 desktop application",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "echo 'Desktop build: run pnpm desktop:bundle for full Tauri build'",
    "bundle": "tauri build",
    "typecheck": "echo 'Typecheck: Rust compilation validates types'",
    "clean": "rimraf src-tauri/target"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "rimraf": "^6.0.1"
  }
}
